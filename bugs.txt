sign in click without filling in fields gives error
should auto-scroll to bottom of messages list on each new incoming message
disconnect from socket when user logs out

extras:
add (real) timestamp (H:MM PM) to chat messages
(real) profile pictures
load list of active channels
click on room name joins that room
use presence to track (# of?) users currently online
/giphy
emoji support
DMs
